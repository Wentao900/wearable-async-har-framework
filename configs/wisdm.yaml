project:
  name: wearable-async-har-framework

data:
  dataset: wisdm
  root: data/WISDM
  window_size: 128
  stride: 64
  modalities:
    - accelerometer

model:
  encoder: tcn
  hidden_dim: 128
  fusion: mean
  alignment_strategy: nearest-neighbor
  reference_timeline_strategy: densest
  use_timestamp_embedding: true
  use_missingness_mask: true
  num_classes: 6

training:
  batch_size: 32
  epochs: 3
  learning_rate: 0.001
  weight_decay: 0.0001
  modality_dropout_prob: 0.0

runtime:
  device: cpu
  num_workers: 0

logging:
  output_dir: outputs/wisdm
